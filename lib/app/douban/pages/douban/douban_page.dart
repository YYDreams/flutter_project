
import 'package:flutter/material.dart';

class DouBanPage extends StatelessWidget {
  const DouBanPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("书影音"),
    );
  }
}

